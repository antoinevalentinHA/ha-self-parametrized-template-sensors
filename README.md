# ha-self-parametrized-template-sensors
Reusable Home Assistant template sensor patterns using YAML anchors and this.entity_id introspection. Write the logic once, reuse it across many entities, and avoid configuration duplication.
